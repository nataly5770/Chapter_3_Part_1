package NumericLiterals_2_10;

public class IntegerLiterals {
    public static void main(String[] args) {
        System.out.println(0B1111);
        System.out.println(07777);
        System.out.println(0XFFFF);
        System.out.println(06666);


        /*

        2.10.1 How many accurate digits are stored in a float or double type variable?
        In float 8 digits and in double type variable 16 digits


        2.10.2 Which of the following are correct literals for floating-point numbers?
        12.3(this is a double value), 12.3e+2 (yes), 23.4e-2(yes), –334.4 (negative double), 20.5 (double), 39F (yes), 40D (double)


        2.10.3 Which of the following are the same as 52.534? (I did not know how to calculate)
        5.2534e+1, 0.52534e+2, 525.34e-1, 5.2534e+0

        2.10.4 Which of the following are correct literals?
        5_2534e+1 (correct), _2534 >(wrong as it cannot start from the underscore), 5_2(correct), 5_(no point to point to
        set an underscore at the end if there is just one digit)

         */
    }
}
